/**
 * @file Contém as constantes utilizadas na aplicação.
 */

/**
 * Chave para o cache de repositórios no sessionStorage.
 * @type {string}
 */
export const CACHE_KEY = "github-portfolio-repos";

/**
 * Tempo de vida do cache em milissegundos (15 minutos).
 * @type {number}
 */
export const CACHE_TTL = 15 * 60 * 1000;
